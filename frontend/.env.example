# In production, set this to the deployed FastAPI backend's base
# URL (no trailing slash, no /analyze). Falls back to
# http://localhost:8000 if unset, which is fine for local dev.
NEXT_PUBLIC_API_URL=
